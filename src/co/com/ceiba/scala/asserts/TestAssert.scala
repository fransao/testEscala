package co.com.ceiba.scala.asserts

object TestAssert extends App {
  val left = 2
  val right = 1
//  assert(left == right)
  
  val someValue: Option[String] = Some("I am wrapped in something")
  
  
}